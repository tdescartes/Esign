{
  "name": "esign-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "inkwell/frontend",
    "inkwell/server",
    "inkwell/shared"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace inkwell/frontend",
    "dev:api": "npm run dev --workspace inkwell/server",
    "build:web": "npm run build --workspace inkwell/frontend",
    "test": "npm run test --workspace inkwell/server && npm run test --workspace inkwell/frontend",
    "lint": "npm run lint --workspace inkwell/server && npm run lint --workspace inkwell/frontend"
  }
}
